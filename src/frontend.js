// frontend.js — HTML + سكريبت منفصل، بدون backticks نهائيًا
export const INDEX_HTML = "<!DOCTYPE html>\n<html lang=\"ar\" dir=\"rtl\">\n<head>\n<meta charset=\"utf-8\">\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n<title>التوأم الرقمي — ترعة الإسماعيلية</title>\n<style>\n:root{--navy:#0e3b5c;--navy2:#155083;--bg:#f4f7fb;--card:#fff;--ok:#1a7f37;--okbg:#ecfdf3;--warn:#b54708;--warnbg:#fff7ed;--danger:#b42318;--dangerbg:#fff1f0;--line:#e2e8f0;--line2:#cbd5e1;--ink:#0f172a;--ink2:#475569;--ink3:#94a3b8;--cyan:#0891b2}\n*{box-sizing:border-box;font-family:system-ui,\"Segoe UI\",Tahoma,sans-serif}\nbody{margin:0;background:var(--bg);color:var(--ink)}\nheader{background:linear-gradient(135deg,var(--navy),var(--navy2));color:#fff;padding:14px 18px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:10px;box-shadow:0 2px 12px rgba(14,59,92,.18)}\nheader h1{font-size:18px;margin:0;font-weight:700}\nheader input[type=date]{padding:7px 12px;border-radius:9px;border:none;font-size:14px}\n.tabs{display:flex;gap:6px;padding:10px 14px;background:#fff;border-bottom:1px solid var(--line);overflow-x:auto;position:sticky;top:0;z-index:5}\n.tabs button{padding:8px 15px;border:none;background:#eef2f6;border-radius:20px;cursor:pointer;white-space:nowrap;font-size:14px;font-weight:600;color:var(--ink2);transition:.15s}\n.tabs button.active{background:var(--navy);color:#fff}\nmain{padding:14px;max-width:1040px;margin:auto}\n.kpibar{display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:10px;margin-bottom:14px}\n.kpi{background:#fff;border:1px solid var(--line);border-radius:14px;padding:12px 14px;text-align:center;box-shadow:0 1px 3px rgba(0,0,0,.04)}\n.kpi .v{font-size:22px;font-weight:800;color:var(--navy);line-height:1.1}\n.kpi .l{font-size:11px;color:var(--ink3);margin-top:4px}\n.prog{height:8px;background:#e8eef5;border-radius:99px;overflow:hidden;margin:10px 0 16px}\n.prog>i{display:block;height:100%;background:linear-gradient(90deg,var(--ok),#34d399);transition:width .4s}\n.pills{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:14px;align-items:center}\n.pill{padding:6px 14px;border:1px solid var(--line2);background:#fff;border-radius:20px;cursor:pointer;font-size:13px;font-weight:600;color:var(--ink2)}\n.pill.active{background:var(--navy);color:#fff;border-color:var(--navy)}\n.rcard{background:#fff;border:1px solid var(--line);border-radius:16px;padding:14px 16px;margin-bottom:12px;box-shadow:0 1px 3px rgba(0,0,0,.05);transition:.15s}\n.rcard.filled{border-color:#bbf7d0;background:linear-gradient(180deg,#fff,#f6fdf9)}\n.rcard .top{display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:6px}\n.rcard h3{margin:0;color:var(--navy);font-size:15.5px;font-weight:700}\n.rcard .km{color:var(--ink3);font-size:12px;font-weight:500;font-family:ui-monospace,monospace}\n.rcard .badge{font-size:10.5px;padding:2px 9px;border-radius:20px;font-weight:600}\n.rcard .badge.reg{background:#e0f2fe;color:#0369a1}.rcard .badge.intk{background:#f0fdf4;color:#15803d}.rcard .badge.mouth{background:#fef3c7;color:#92400e}\n.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(110px,1fr));gap:9px;margin-top:11px}\n.grid label{font-size:11.5px;color:var(--ink2);display:block;margin-bottom:3px;font-weight:500}\n.grid input{width:100%;padding:8px 9px;border:1px solid var(--line2);border-radius:9px;font-size:14px;transition:.15s}\n.grid input:focus{outline:none;border-color:var(--cyan);box-shadow:0 0 0 3px rgba(8,145,178,.1)}\n.qrow{margin-top:12px;display:flex;align-items:center;flex-wrap:wrap;gap:8px;padding-top:11px;border-top:1px solid var(--line)}\n.qrow .qv{font-size:17px;font-weight:800}\n.qrow .qv.calibrated{color:var(--ok)}.qrow .qv.manning{color:var(--warn)}.qrow .qv.manual{color:var(--navy)}\n.qrow .meth{font-size:11px;padding:2px 9px;border-radius:20px;background:#eef2f6;color:var(--ink2);font-weight:600}\n.qrow .err{font-size:10.5px;padding:2px 9px;border-radius:20px;background:#fff7ed;color:var(--warn)}\n.qrow .dev{font-size:11px;padding:2px 9px;border-radius:20px;font-weight:600}\n.save{margin-inline-start:auto;background:var(--ok);color:#fff;border:none;padding:8px 18px;border-radius:10px;cursor:pointer;font-size:13px;font-weight:700}\n.save:active{transform:scale(.97)}\n.toolbar{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px;align-items:center}\n.btn{padding:8px 14px;border:1px solid var(--line2);background:#fff;border-radius:10px;cursor:pointer;font-size:13px;font-weight:600;color:var(--ink2)}\n.btn.navy{background:var(--navy);color:#fff;border-color:var(--navy)}.btn.ok{background:var(--ok);color:#fff;border-color:var(--ok)}\n.toolbar input[type=search]{padding:8px 12px;border:1px solid var(--line2);border-radius:10px;font-size:13px;flex:1;min-width:130px}\n.card{background:#fff;border:1px solid var(--line);border-radius:16px;padding:14px 16px;margin-bottom:12px;box-shadow:0 1px 3px rgba(0,0,0,.05)}\n.card h3{margin:0 0 10px;color:var(--navy);font-size:16px}\n.tbl-wrap{overflow-x:auto;border-radius:10px;border:1px solid var(--line)}\ntable{width:100%;border-collapse:collapse;font-size:13px}\nth{background:var(--navy);color:#fff;padding:9px 10px;text-align:right;font-weight:600;white-space:nowrap}\ntd{border-bottom:1px solid var(--line);padding:8px 10px;text-align:right;white-space:nowrap}\ntr:last-child td{border-bottom:none}\ntbody tr:nth-child(even){background:#fafcfe}\ntd.num{font-family:ui-monospace,monospace}\n.del{background:transparent;border:1px solid var(--line);color:var(--danger);padding:4px 9px;border-radius:7px;cursor:pointer;font-size:11px;font-weight:600}\n.balance{display:flex;gap:12px;flex-wrap:wrap;margin-top:8px}\n.balance .box{flex:1;min-width:110px;text-align:center;padding:14px;border-radius:13px;background:#eef2f6}\n.balance .box b{display:block;font-size:24px;margin:4px 0}\n.balance .box span{font-size:12px;color:var(--ink2)}\n.hidden{display:none}.muted{color:var(--ink3);font-size:13px}\n</style></head>\n<body>\n<header><h1>🌊 التوأم الرقمي — ترعة الإسماعيلية</h1><input type=\"date\" id=\"dateSel\"></header>\n<div class=\"tabs\">\n<button class=\"active\" data-tab=\"readings\">📋 إدخال اليوم</button>\n<button data-tab=\"daily\">📊 السجل اليومي</button>\n<button data-tab=\"balance\">⚖️ الميزان الكتلي</button>\n<button data-tab=\"audit\">🕓 سجل التغييرات</button>\n</div>\n<main>\n<div id=\"tab-readings\"></div>\n<div id=\"tab-daily\" class=\"hidden\"></div>\n<div id=\"tab-balance\" class=\"hidden\"></div>\n<div id=\"tab-audit\" class=\"hidden\"></div>\n</main>\n<script src=\"https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js\"></script>\n<script src=\"/app.js\"></script>\n</body></html>";
export const APP_JS = "const API = location.origin + '/api';\nconst dateSel = document.getElementById('dateSel');\ndateSel.value = new Date().toISOString().slice(0,10);\nlet NODES = [];\nlet CURRENT = {};\nvar nodeFilter = 'main';\nconst mName = {manual:'يدوي', calibrated:'📐 معاير', manning:'📏 مانينج'};\nconst kindBadge = {mouth:['فم','mouth'], regulator:['حجز','reg'], intake:['فرع','intk']};\n\nfunction calibQ(node, r){\n  if(node.curve_type === 'power'){\n    var head = (r.wl_dn == null ? 0 : r.wl_dn) - node.h0;\n    return head <= 0 ? 0 : node.a * Math.pow(head, node.b);\n  }\n  if(node.curve_type === 'linear'){\n    return node.c_n*(r.gates_open||0) + node.c_up*(r.wl_up||0) + node.c_dn*(r.wl_dn||0) + node.intercept;\n  }\n  return null;\n}\nfunction manning(node, r){\n  var y = (r.wl_dn||0) - (node.bed_level||0);\n  if(y <= 0) return 0;\n  var b = node.gauge_width != null ? node.gauge_width : node.bed_width;\n  var z = node.gauge_slope != null ? node.gauge_slope : node.side_slope;\n  var A = (b + z*y)*y, P = b + 2*y*Math.sqrt(1+z*z), R = A/P;\n  return (1/node.manning_n) * A * Math.pow(R, 2/3) * Math.sqrt(node.bed_slope);\n}\nfunction resolveQ(node, r){\n  if(r.q_manual != null && r.q_manual !== '') return {q:+r.q_manual, m:'manual'};\n  var c = calibQ(node, r);\n  if(c != null && isFinite(c) && c >= 0) return {q:c, m:'calibrated'};\n  return {q:manning(node, r), m:'manning'};\n}\nfunction designFront(node){ return node.bed_level != null ? node.bed_level + 5 : null; }\n\nfunction el(tag, cls, txt){\n  var e = document.createElement(tag);\n  if(cls) e.className = cls;\n  if(txt != null) e.textContent = txt;\n  return e;\n}\nfunction field(label, key, val, step){\n  var d = el('div');\n  d.appendChild(el('label', null, label));\n  var i = document.createElement('input');\n  i.type = 'number'; if(step) i.step = step;\n  i.dataset.f = key; i.value = (val == null ? '' : val);\n  d.appendChild(i);\n  return d;\n}\n\nasync function loadNodes(){ NODES = await (await fetch(API+'/nodes')).json(); }\nasync function loadReadings(){\n  var data = await (await fetch(API+'/readings?date='+dateSel.value)).json();\n  CURRENT = {};\n  data.nodes.forEach(function(n){ CURRENT[n.id] = n; });\n  renderReadings();\n}\n\nfunction renderReadings(){\n  var wrap = document.getElementById('tab-readings');\n  wrap.innerHTML = '';\n\n  // ===== شريط KPIs =====\n  var entered = 0, up = 0, dn = 0, warn = 0;\n  NODES.forEach(function(node){\n    var r = CURRENT[node.id] || {};\n    var has = (r.wl_dn != null) || (r.wl_up != null) || (r.q_manual != null);\n    if(has) entered++;\n    var df = designFront(node);\n    if(df != null && r.wl_up != null){\n      var dev = r.wl_up - df;\n      if(dev > 0.02) up++; else if(dev < -0.02) dn++;\n      if(Math.abs(dev) > 0.3) warn++;\n    }\n  });\n  var kb = el('div','kpibar');\n  function kpi(v, l, col){\n    var d = el('div','kpi');\n    var vv = el('div','v', v); if(col) vv.style.color = col;\n    d.appendChild(vv); d.appendChild(el('div','l', l)); return d;\n  }\n  kb.appendChild(kpi(entered + '/' + NODES.length, 'محطات مُدخلة'));\n  kb.appendChild(kpi(up || '—', 'ارتفاع مناسيب', '#16a34a'));\n  kb.appendChild(kpi(dn || '—', 'انخفاض مناسيب', '#dc2626'));\n  kb.appendChild(kpi(warn || '—', 'تحذيرات', '#d97706'));\n  wrap.appendChild(kb);\n\n  // ===== شريط التقدّم =====\n  var pct = NODES.length ? Math.round(entered / NODES.length * 100) : 0;\n  var pr = el('div','prog'); var pi = el('i'); pi.style.width = pct + '%'; pr.appendChild(pi);\n  wrap.appendChild(pr);\n\n  // ===== فلتر النوع =====\n  var pills = el('div','pills');\n  [['main','الحجوز الرئيسية'],['intake','الفروع والمآخذ'],['all','الكل']].forEach(function(f){\n    var b = el('button','pill'+(nodeFilter===f[0]?' active':''), f[1]);\n    b.onclick = function(){ nodeFilter = f[0]; renderReadings(); };\n    pills.appendChild(b);\n  });\n  wrap.appendChild(pills);\n\n  // ===== البطاقات =====\n  var list = NODES.filter(function(n){\n    if(nodeFilter==='all') return true;\n    if(nodeFilter==='intake') return n.kind==='intake';\n    return n.kind!=='intake';\n  });\n  list.forEach(function(node){\n    var r = CURRENT[node.id] || {};\n    var res = resolveQ(node, r);\n    var filled = (r.wl_dn != null) || (r.wl_up != null) || (r.q_manual != null);\n    var card = el('div','rcard' + (filled ? ' filled' : ''));\n    // الرأس\n    var top = el('div','top');\n    var hl = el('div');\n    var h = el('h3', null, node.name_ar);\n    hl.appendChild(h);\n    hl.appendChild(el('span','km', 'كم ' + node.km));\n    top.appendChild(hl);\n    var bd = kindBadge[node.kind] || ['—','intk'];\n    top.appendChild(el('span','badge ' + bd[1], bd[0]));\n    card.appendChild(top);\n    // الحقول\n    var grid = el('div','grid');\n    grid.appendChild(field('منسوب أمام', 'wl_up', r.wl_up, '0.01'));\n    grid.appendChild(field('منسوب خلف', 'wl_dn', r.wl_dn, '0.01'));\n    if(node.curve_type === 'linear') grid.appendChild(field('عدد الحب المرفوع', 'gates_open', r.gates_open));\n    grid.appendChild(field('تصرف يدوي (اختياري)', 'q_manual', r.q_manual, '0.01'));\n    card.appendChild(grid);\n    // صف التصرف\n    var qrow = el('div','qrow');\n    qrow.appendChild(el('span',null,'التصرف:'));\n    var qv = el('span','qv ' + res.m, res.q.toFixed(3) + ' م³/ث');\n    qrow.appendChild(qv);\n    var meth = el('span','meth', mName[res.m]);\n    qrow.appendChild(meth);\n    if(node.avg_error_pct) qrow.appendChild(el('span','err','خطأ معايرة ' + node.avg_error_pct + '%'));\n    var devEl = el('span','dev'); qrow.appendChild(devEl);\n    var btn = el('button','save','💾 حفظ');\n    qrow.appendChild(btn);\n    card.appendChild(qrow);\n    var inputs = card.querySelectorAll('input');\n    function updateDev(rr){\n      var df = designFront(node);\n      if(df != null && rr.wl_up != null && rr.wl_up !== ''){\n        var dev = (+rr.wl_up) - df;\n        devEl.textContent = (dev>0?'+':'') + dev.toFixed(2) + ' م';\n        var ad = Math.abs(dev);\n        devEl.style.background = ad>0.3?'#fff1f0':ad>0.1?'#fff7ed':'#ecfdf3';\n        devEl.style.color = ad>0.3?'#b42318':ad>0.1?'#b54708':'#1a7f37';\n        devEl.style.display = '';\n      } else { devEl.style.display = 'none'; }\n    }\n    function recalc(){\n      var rr = {};\n      inputs.forEach(function(i){ rr[i.dataset.f] = i.value === '' ? null : +i.value; });\n      var x = resolveQ(node, rr);\n      qv.textContent = x.q.toFixed(3) + ' م³/ث';\n      qv.className = 'qv ' + x.m;\n      meth.textContent = mName[x.m];\n      updateDev(rr);\n    }\n    inputs.forEach(function(i){ i.addEventListener('input', recalc); });\n    updateDev(r);\n    btn.onclick = async function(){\n      var body = {node_id:node.id, reading_date:dateSel.value};\n      inputs.forEach(function(i){ body[i.dataset.f] = i.value === '' ? null : +i.value; });\n      btn.textContent = '⏳ جاري...';\n      await fetch(API+'/readings', {method:'POST', headers:{'content-type':'application/json'}, body:JSON.stringify(body)});\n      btn.textContent = '✓ تم';\n      setTimeout(loadReadings, 400);\n    };\n    wrap.appendChild(card);\n  });\n}\n\n// ============ السجل اليومي ============\nvar dailyFilter = 'all', dailySearch = '';\nasync function loadDaily(){\n  var qs = '?filter=' + dailyFilter + (dailySearch ? '&search=' + encodeURIComponent(dailySearch) : '');\n  var data = await (await fetch(API + '/readings/all' + qs)).json();\n  renderDaily(data);\n}\nfunction renderDaily(data){\n  var wrap = document.getElementById('tab-daily');\n  wrap.innerHTML = '';\n  // KPIs ملخص\n  var kb = el('div','kpibar');\n  function kpi(v,l){ var d=el('div','kpi'); d.appendChild(el('div','v',v)); d.appendChild(el('div','l',l)); return d; }\n  kb.appendChild(kpi(data.total||0,'إجمالي القراءات'));\n  kb.appendChild(kpi(data.last_name||'—','آخر محطة'));\n  kb.appendChild(kpi(data.last_date||'—','آخر تاريخ'));\n  wrap.appendChild(kb);\n  var c = el('div','card');\n  c.appendChild(el('h3',null,'📊 السجل اليومي للقراءات'));\n  // أدوات\n  var tb = el('div','toolbar');\n  [['all','الكل'],['today','اليوم'],['week','أسبوع'],['month','شهر']].forEach(function(f){\n    var b = el('button','pill'+(dailyFilter===f[0]?' active':''), f[1]);\n    b.onclick = function(){ dailyFilter=f[0]; loadDaily(); };\n    tb.appendChild(b);\n  });\n  var srch = document.createElement('input');\n  srch.type='search'; srch.placeholder='بحث باسم المحطة أو التاريخ...'; srch.value=dailySearch;\n  srch.oninput = function(){ dailySearch=srch.value; clearTimeout(srch._t); srch._t=setTimeout(loadDaily,350); };\n  tb.appendChild(srch);\n  var imp = el('button','btn navy','⬆️ استيراد Excel'); imp.onclick=function(){ document.getElementById('xlsImport').click(); }; tb.appendChild(imp);\n  var exp = el('button','btn ok','⬇️ تصدير Excel'); exp.onclick=function(){ exportDaily(data.rows); }; tb.appendChild(exp);\n  c.appendChild(tb);\n  var fi = document.createElement('input'); fi.type='file'; fi.id='xlsImport'; fi.accept='.xlsx,.xls,.csv'; fi.style.display='none';\n  fi.onchange = function(e){ importExcel(e.target.files[0]); }; c.appendChild(fi);\n  // الجدول\n  var tw = el('div','tbl-wrap');\n  var t = document.createElement('table');\n  t.innerHTML = '<thead><tr><th>التاريخ</th><th>المحطة</th><th>كم</th><th>أمام</th><th>خلف</th><th>الفرق</th><th></th></tr></thead>';\n  var tb2 = document.createElement('tbody');\n  if(data.rows.length){\n    data.rows.forEach(function(r){\n      var tr = document.createElement('tr');\n      tr.appendChild(el('td','num',r.reading_date||'—'));\n      tr.appendChild(el('td',null,r.name_ar||'—'));\n      tr.appendChild(el('td','num',r.km!=null?r.km:'—'));\n      tr.appendChild(el('td','num',r.wl_up!=null?(+r.wl_up).toFixed(2):'—'));\n      tr.appendChild(el('td','num',r.wl_dn!=null?(+r.wl_dn).toFixed(2):'—'));\n      var dh = (r.wl_up!=null&&r.wl_dn!=null)?(r.wl_up-r.wl_dn).toFixed(2):'—';\n      tr.appendChild(el('td','num',dh));\n      var dtd = el('td'); var db = el('button','del','حذف');\n      db.onclick = async function(){ if(confirm('حذف هذه القراءة؟')){ await fetch(API+'/readings?id='+r.id,{method:'DELETE'}); loadDaily(); } };\n      dtd.appendChild(db); tr.appendChild(dtd);\n      tb2.appendChild(tr);\n    });\n  } else {\n    var tr=document.createElement('tr'); var td=el('td','muted','لا توجد قراءات — أدخل من تبويب الإدخال أو ارفع Excel');\n    td.colSpan=7; td.style.textAlign='center'; td.style.padding='28px'; tr.appendChild(td); tb2.appendChild(tr);\n  }\n  t.appendChild(tb2); tw.appendChild(t); c.appendChild(tw);\n  wrap.appendChild(c);\n}\nfunction exportDaily(rows){\n  if(!rows||!rows.length){ alert('لا توجد بيانات للتصدير'); return; }\n  var aoa=[['التاريخ','المحطة','كم','منسوب أمام','منسوب خلف','عدد الحب','تصرف يدوي']];\n  rows.forEach(function(r){ aoa.push([r.reading_date,r.name_ar,r.km,r.wl_up,r.wl_dn,r.gates_open,r.q_manual]); });\n  var ws=XLSX.utils.aoa_to_sheet(aoa); var wb=XLSX.utils.book_new();\n  XLSX.utils.book_append_sheet(wb,ws,'القراءات'); XLSX.writeFile(wb,'قراءات_الإسماعيلية.xlsx');\n}\nfunction matchNode(name,km){\n  var nm=(name||'').replace(/\\s+/g,'');\n  for(var i=0;i<NODES.length;i++){ var n=NODES[i];\n    if(km!=null && Math.abs(n.km-km)<0.6) return n.id;\n    if(nm && n.name_ar.replace(/\\s+/g,'').indexOf(nm)>=0) return n.id;\n    if(nm && nm.indexOf(n.name_ar.replace(/\\s+/g,''))>=0) return n.id;\n  }\n  return null;\n}\nfunction normHeader(h){\n  var s=String(h==null?'':h).trim().toLowerCase().replace(/\\s+/g,'');\n  if(/(km|كم|كيلو)/.test(s))return 'km';\n  if(/(name|اسم|محطه|محطة|نقطه)/.test(s))return 'name';\n  if(/(date|تاريخ|يوم)/.test(s))return 'date';\n  if(/(امام|front|اماميه)/.test(s))return 'front';\n  if(/(خلف|back|خلفيه)/.test(s))return 'back';\n  if(/(حب|gates|بوابات)/.test(s))return 'gates';\n  return '';\n}\nfunction excelDate(v){\n  if(v==null||v==='')return '';\n  if(typeof v==='number'&&isFinite(v)){ var d=new Date((v-25569)*86400*1000); return d.toISOString().slice(0,10); }\n  var s=String(v).trim();\n  if(/^\\d{4}-\\d{2}-\\d{2}/.test(s))return s.slice(0,10);\n  var m=s.match(/^(\\d{1,2})[\\/\\-.](\\d{1,2})[\\/\\-.](\\d{2,4})/);\n  if(m){ var y=m[3]; if(y.length===2)y='20'+y; return y+'-'+m[2].padStart(2,'0')+'-'+m[1].padStart(2,'0'); }\n  return s;\n}\nfunction importExcel(file){\n  if(!file)return;\n  var reader=new FileReader();\n  reader.onload=async function(e){\n    try{\n      var wb=XLSX.read(e.target.result,{type:'array'});\n      var ws=wb.Sheets[wb.SheetNames[0]];\n      var aoa=XLSX.utils.sheet_to_json(ws,{header:1,defval:null});\n      var hdr=-1,cols={};\n      for(var i=0;i<Math.min(aoa.length,10);i++){ var map={},hits=0;\n        aoa[i].forEach(function(c,j){ var k=normHeader(c); if(k){ map[k]=j; hits++; } });\n        if(hits>=2){ hdr=i; cols=map; break; } }\n      if(hdr<0){ alert('تعذّر التعرف على أعمدة الملف. تأكد من: التاريخ، الاسم/كم، أمام، خلف'); return; }\n      var rows=[],skip=0;\n      for(var r=hdr+1;r<aoa.length;r++){ var row=aoa[r]; if(!row)continue;\n        var name=cols.name!=null?row[cols.name]:null;\n        var km=cols.km!=null?parseFloat(row[cols.km]):null;\n        var nid=matchNode(name,isNaN(km)?null:km);\n        if(!nid){ skip++; continue; }\n        var date=cols.date!=null?excelDate(row[cols.date]):'';\n        if(!date){ skip++; continue; }\n        rows.push({node_id:nid,reading_date:date,\n          wl_up:cols.front!=null?parseFloat(row[cols.front]):null,\n          wl_dn:cols.back!=null?parseFloat(row[cols.back]):null,\n          gates_open:cols.gates!=null?parseInt(row[cols.gates],10):null});\n      }\n      if(!rows.length){ alert('لم يتم مطابقة أي صف بالمحطات المعروفة'); return; }\n      var res=await(await fetch(API+'/readings/bulk',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(rows)})).json();\n      alert('تم استيراد '+(res.saved||0)+' قراءة'+(skip?(' · تخطّي '+skip+' صف'):''));\n      loadDaily();\n    }catch(err){ alert('خطأ في قراءة الملف: '+err.message); }\n  };\n  reader.readAsArrayBuffer(file);\n}\n\n// ============ الميزان الكتلي ============\nasync function loadBalance(){\n  var b = await (await fetch(API+'/balance?date='+dateSel.value)).json();\n  var wrap = document.getElementById('tab-balance');\n  wrap.innerHTML = '';\n  var c1 = el('div','card');\n  c1.appendChild(el('h3',null,'⚖️ الميزان الكتلي — '+b.date));\n  var bal = el('div','balance');\n  function box(label,val,bg,col){ var d=el('div','box'); d.style.background=bg||'#eef2f6';\n    d.appendChild(el('span',null,label)); var bb=el('b',null,val); if(col)bb.style.color=col; else bb.style.color='var(--navy)';\n    d.appendChild(bb); d.appendChild(el('span',null,'م³/ث')); return d; }\n  bal.appendChild(box('الداخل',b.inflow));\n  bal.appendChild(box('الخارج',b.outflow));\n  var warn=Math.abs(b.gap)>1;\n  bal.appendChild(box('الفجوة',b.gap,warn?'#fff1f0':'#ecfdf3',warn?'#b42318':'#1a7f37'));\n  c1.appendChild(bal); wrap.appendChild(c1);\n  var c2=el('div','card'); var tw=el('div','tbl-wrap'); var t=document.createElement('table');\n  t.innerHTML='<thead><tr><th>العقدة</th><th>النوع</th><th>التصرف م³/ث</th></tr></thead>';\n  var tb=document.createElement('tbody');\n  b.rows.forEach(function(row){ var tr=document.createElement('tr');\n    tr.appendChild(el('td',null,row.name_ar)); tr.appendChild(el('td',null,row.kind)); tr.appendChild(el('td','num',row.q));\n    tb.appendChild(tr); });\n  t.appendChild(tb); tw.appendChild(t); c2.appendChild(tw); wrap.appendChild(c2);\n}\n\n// ============ سجل التغييرات ============\nasync function loadAudit(){\n  var a = await (await fetch(API+'/audit?limit=50')).json();\n  var wrap = document.getElementById('tab-audit'); wrap.innerHTML='';\n  var c=el('div','card'); c.appendChild(el('h3',null,'🕓 آخر التغييرات'));\n  if(a.length){ var tw=el('div','tbl-wrap'); var t=document.createElement('table');\n    t.innerHTML='<thead><tr><th>الوقت</th><th>الإجراء</th><th>العقدة</th></tr></thead>';\n    var tb=document.createElement('tbody');\n    a.forEach(function(x){ var tr=document.createElement('tr');\n      tr.appendChild(el('td','num',x.ts)); tr.appendChild(el('td',null,x.action)); tr.appendChild(el('td',null,x.node_id||'-'));\n      tb.appendChild(tr); });\n    t.appendChild(tb); tw.appendChild(t); c.appendChild(tw);\n  } else { c.appendChild(el('p','muted','لا يوجد سجل بعد')); }\n  wrap.appendChild(c);\n}\n\ndocument.querySelectorAll('.tabs button').forEach(function(btn){\n  btn.onclick = function(){\n    document.querySelectorAll('.tabs button').forEach(function(b){ b.classList.remove('active'); });\n    btn.classList.add('active');\n    ['readings','daily','balance','audit'].forEach(function(t){ document.getElementById('tab-'+t).classList.add('hidden'); });\n    var t=btn.dataset.tab;\n    document.getElementById('tab-'+t).classList.remove('hidden');\n    if(t==='daily')loadDaily();\n    if(t==='balance')loadBalance();\n    if(t==='audit')loadAudit();\n  };\n});\ndateSel.onchange = loadReadings;\n(async function(){ await loadNodes(); await loadReadings(); })();\n";
